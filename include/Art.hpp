#pragma once
#include <vector>
#include <string>

/**
 * @brief ASCII Art for the custom "BARTY" logo.
 */
extern const std::vector<std::string> BARTY_ART;

/**
 * @brief ASCII Art for the official Pop!_OS logo (Neofetch style).
 */
extern const std::vector<std::string> POP_ART;

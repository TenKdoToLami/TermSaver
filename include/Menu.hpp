#pragma once

/**
 * @brief Displays the interactive selection menu.
 * 
 * Loops until the user selects an option or exits.
 * 
 * @return int The index of the selected animation, or -1 if the user quits.
 */
int show_menu();
